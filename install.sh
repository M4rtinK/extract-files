#!/bin/bash

# Installs excp and exmv to a folder that is in PATH
# NOTE: you need to be root to run this

ln -s excp /usr/bin/excp
ln -s exmv /usr/bin/excp
echo "excp and exmv added to path (/usr/bin/)